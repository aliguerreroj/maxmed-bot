/**
 * Telegram bot entrypoint — Phase 4.
 * grammy long-polling, wired to the extraction + pricing + phrasing pipeline.
 */

async function main(): Promise<void> {
  console.log("Bot not yet implemented — Phase 4.");
  process.exit(0);
}

main();

export {};
